#include <bits/stdc++.h>

#include <iostream>
using namespace std;

typedef unsigned long long ll;


#define rep(i, from, to) for (ll i = from; i <= (ll)(to); i++)

const int MAX_N = 1E5;

int main(void) {

    int T;
    cin >> T;

    rep(i, 1, T) {
        int n;
        string name;
        cin >> n >> name;

        vector<int> s(n / 2 + 2);
    }
}