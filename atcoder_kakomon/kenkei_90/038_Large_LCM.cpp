#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#include <cassert>
#include <numeric>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

typedef unsigned long long ll;

ll gcd(ll a,ll b){
    if (b == 0)
        return a;
    return gcd(b, a % b);
}

int main(void)
{
    
}